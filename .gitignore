# --- Excluded sub-workspaces / unrelated subprojects ---
.claude/
!data_pipeline/.claude/
.mcp.json
.obsidian/
Clippings/
tunnels/
team_repo/
etolb/
repo_res/

# --- Large / binary data ---
*.parquet
*.pdf

# --- Generated experiment artifacts ---
# Matches both the repo-root runs/ (pre-reorg configs) and forecasting/runs/ (current
# configs, output_dir: runs resolved relative to forecasting/ as cwd) -- nothing under either
# is tracked; result numbers belong in the relevant scratchpad .md, not raw run output.
.tmp.driveupload/
tmp/
runs/
forecasting/runs/

# --- Paused/unfinished parallel track, kept local-only (2026-09-18) ---
# AutoGluon fine-tuning experiment: raw, outdated, not being actively worked on. Kept on disk
# for reference but deliberately not published. Open blocker: intraday AutoGluon
# index-alignment bug, no plan to resume it.
finetuning_wip/

# --- Historical/archived material, kept local-only (2026-09-18) ---
# User's explicit choice: no archive material of any kind goes to GitHub, even though it's
# real historical record (old raw-close pre-close_adj runs, old numbered stage_N configs,
# legacy notebooks, superseded session-log docs). Kept on disk for local reference only.
docs/archive/
forecasting/archive/
data_pipeline/archive/

# --- Working/process notes, kept local-only (2026-09-18) ---
# Same reasoning as archive/ above -- pre-registration + run-log scratchpads are working
# process notes, not polished reader-facing content. Real results go in FINDINGS.md instead.
forecasting/scratchpads/

# --- Pasted Obsidian images ---
Pasted image*

# --- Files not part of the Chronos experiment ---
ArenaGo test.md
m_arenago.txt
misha-gitlab-mhack.txt
sec.json
ass.json
engines.json
chat.md

# --- Python artifacts ---
__pycache__/
*.pyc
*.pyo
.venv/
.venv*/
*.egg-info/

# --- Jupyter checkpoints ---
.ipynb_checkpoints/

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini
gp.md

# --- Local market-data caches (ISS index/key-rate/futures pulls): raw data, never published ---
forecasting/moex_cache/
