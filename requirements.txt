# Direct dependencies, pinned to the versions every result in this repo was produced with (Python 3.12).
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# The Colab LoRA notebook installs its own pins (chronos-forecasting 2.3.2 + peft; see
# forecasting/risk_experiment/colab/build_notebook.py).

# data and statistics
numpy==2.5.3
pandas==3.0.5
pyarrow==25.0.1
scipy==1.18.1
statsmodels==0.15.0
arch==8.0.0

# forecasting model
chronos-forecasting==2.3.2
torch==2.14.0
transformers==5.17.0
accelerate==1.15.0
peft==0.21.0

# data pipeline (MOEX AlgoPack / ISS)
requests==2.34.2
certifi==2026.7.22
PyYAML==6.0.3
tqdm==4.70.1

# notebooks, figures, tests
nbformat==5.11.1
ipython==9.17.1
matplotlib==3.11.2
pytest==9.1.1
